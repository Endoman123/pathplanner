subprojects {
    apply plugin: 'application'
    apply plugin: 'java'

    repositories {
        mavenCentral()
        maven {
            url 'http://dev.imjac.in/maven/'
        }
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '4.7'
}